<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyFirstMAUIApp.View.SliderPage"
             Title="{Binding Title}">
    <VerticalStackLayout>



        <Grid Margin="50, 50, 50, 50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0"
                    Grid.Column="0"
                    Text="{Binding Xaml}"
                    Style="{StaticResource TileButton}"
                    Command="{Binding SliderXAMLClickedCommand}"/>


            <Button Grid.Row="0"
                    Grid.Column="1"
                    Text="{Binding Vm}"
                    Style="{StaticResource TileButton}"
                    Command="{Binding SliderVMClickedCommand}"/>


        </Grid>

    </VerticalStackLayout>
</ContentPage>