<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyFirstMAUIApp.View.ImagesPage"
             Title="{Binding Images}">
    <VerticalStackLayout>
        <Grid Margin="50, 50, 50, 50">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0"
	                Grid.Column="0"
	                Text="{Binding UriImages}"
	                Style="{StaticResource TileButton}"
                    Command="{Binding URIImagesClickedCommand}"/>


            <Button Grid.Row="0"
                    Grid.Column="1"
                    Text="{Binding EmbeddedImages}"
                    Style="{StaticResource TileButton}"
                    Command="{Binding EmbeddedImageClickedCommand}"/>

            <Button Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="{Binding ActivityLoader}"
                    Style="{StaticResource TileButton}"
                    Command="{Binding ActivityLoaderClickedCommand}"/>




        </Grid>
    </VerticalStackLayout>
</ContentPage>